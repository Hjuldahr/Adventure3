package com.example.adventure.categories;

public enum DefenceBypass {
    NONE, // no bypass
    ARMOUR, // bypasses only armour bonuses
    ALL // bypasses all defence bonuses but not elemental resistances
}
